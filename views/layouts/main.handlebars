<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tech Blog</title>
  <link rel="stylesheet" href="/css/jass.css">
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>
  <div class="flex-column justify-space-around">
    <header class="display-flex justify-space-between align-center p-2" id="nav-header">
      <h1>
        <span role="img" aria-label="tech">&#9998;</span>
        <a href="/" id="tech-blog-title">Tech Blog</a>
      </h1>
      <nav class="navbar mt-2">
        <!-- Conditionally render profile, login, logout links -->
        {{#if logged_in}}
        <button class="no-button" id="logout" class="logout-link">Log Out</button>
        <a href="/dashboard" class="dashboard-link">Dashboard</a>
        {{else}}
        <a href="/login" class="login-link">Log In</a>
        <a href="/login" class="dashboard-link">Dashboard</a>
        {{/if}}
      </nav>
    </header>
    <main class="container container-fluid mt-5">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>
    <footer>
      
    </footer>
  </div>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>
